import React from 'react'
import PropTypes from 'prop-types'

import Input from '.'

const propTypes = {}
const defaultProps = {}

function InputLayout(props) {
  return (
    <div></div>
  )
}

InputLayout.propTypes = propTypes
InputLayout.defaultProps = defaultProps

export default InputLayout
